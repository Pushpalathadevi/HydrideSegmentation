# Model Assets

Target folders:
- `registry/` metadata for discoverable models
- `weights/` local model files (gitignored in practice)
- `../frozen_checkpoints/` canonical frozen model metadata registry for deployment guidance

Model files should be external to source control unless explicitly approved.
