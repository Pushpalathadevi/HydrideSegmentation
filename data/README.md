# Data Layout

Target folders:
- `raw/` immutable source images
- `interim/` temporary transformed data
- `processed/` curated datasets
- `corrected_exports/` human-corrected samples for retraining
- `sample_images/` bundled public demo images for GUI onboarding

Never commit large proprietary datasets or model weights to git.
